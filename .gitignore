# Dependencies
node_modules/

# Real config contains relay pairing credentials (serverId + public key).
# Do not commit it. Copy config.example.json to ~/.paseo/agent-bridge.json
# and fill it in there.
agent-bridge.json
config.json

# Logs
*.log